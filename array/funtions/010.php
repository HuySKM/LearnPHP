<?php
$stack = array ("Chuoi", "Tao", "Cam", "Nho");
echo "<pre>";
print_r ($stack);
echo "</pre>";

sort($stack);

echo "<pre>";
print_r ($stack);
echo "</pre>";

// Hàm sắp xếp các phần tử theo giá trị (hàm sort)
